<form method="post" action="{{ path('master_asset_user_role_delete', {'Id': asset_user_role.Id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ asset_user_role.Id) }}">
    <button class="btn">Delete</button>
</form>
