<form method="post" action="{{ path('asset_kendaraan_mobil_delete', {'id': asset_kendaraan_mobil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ asset_kendaraan_mobil.id) }}">
    <button class="btn">Delete</button>
</form>
